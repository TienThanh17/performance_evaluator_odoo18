<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_hr_kpi_generate_wizard_form" model="ir.ui.view">
            <field name="name">hr.kpi.generate.wizard.form</field>
            <field name="model">hr.kpi.generate.wizard</field>
            <field name="arch" type="xml">
                <form string="Generate Evaluations">
                    <sheet>
                        <group>
                            <field name="kpi_id" required="1"/>
<!--                            <field name="all_departments"/>-->
                            <field name="department_id" required="1"/>
                        </group>
                        <group>
                            <field name="period" required="1"/>
                            <field name="start_date" required="1"/>
                            <field name="end_date" required="1"/>
                            <field name="deadline" required="1"/>
                        </group>
                    </sheet>
                    <footer>
                        <button string="Generate" type="object" name="action_generate" class="btn-primary"/>
                        <button string="Cancel" special="cancel" class="btn-secondary"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_hr_kpi_generate_wizard" model="ir.actions.act_window">
            <field name="name">Generate Evaluations</field>
            <field name="res_model">hr.kpi.generate.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
            <field name="context">{'default_kpi_id': active_id}</field>
        </record>

    </data>
</odoo>

